# .env - Environment variables for the SMS Classifier App

# --- Required ---
# Your actual OpenAI API key
OPENAI_API_KEY="sk-xxxyyyxzzz"
# --- Optional Overrides (Defaults are used if these are not set) ---

# You can change the GPT model used for fallback classification
GPT_MODEL_NAME="gpt-4o"
# GPT_MODEL_NAME="gpt-4" # Or use GPT-4 if preferred and you have access

# Adjust the confidence threshold for embedding similarity classification
# Default is 0.60
# EMBEDDING_CONFIDENCE_THRESHOLD=0.65

# Adjust sentiment thresholds (example: make it slightly easier to be 'interested')
# Default is 0.6
# SENTIMENT_POSITIVE_THRESHOLD=0.55

# You can override other model names or thresholds defined in the script here too
# SENTIMENT_MODEL_NAME="path/to/a/different/sentiment/model"
# KEYWORD_INTERESTED_CONFIDENCE=0.95
